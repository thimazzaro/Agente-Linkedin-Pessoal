# Web framework
fastapi>=0.115.5
uvicorn[standard]>=0.32.1
jinja2>=3.1.4
python-multipart>=0.0.12

# Scheduler (runs inside the FastAPI process)
apscheduler>=3.10.4
pytz>=2024.2

# Database
sqlalchemy>=2.0.36

# AI / Search
anthropic>=0.40.0
tavily-python>=0.5.0

# Config — pydantic>=2.11 ships cp314 wheels (Python 3.14 compatible)
pydantic>=2.11.0
pyyaml>=6.0.2

# HTTP (LinkedIn API)
requests>=2.32.3

# Env vars loading in local dev
python-dotenv>=1.0.1
